// Gộp thành 1: tái sử dụng UI từ stories/StoryHistory
import StoryHistory from "../stories/StoryHistory";

const StoriesHistory = () => {
  return <StoryHistory />;
};

export default StoriesHistory;